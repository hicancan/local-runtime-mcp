# ChatGPT web acceptance fixture

`visual-check.png` verifies that `image_read` delivers native image content to the model.

Run the check in a fresh conversation and instruct the model to use only `image_read`. The expected value is intentionally omitted so the model cannot infer it from these instructions.
